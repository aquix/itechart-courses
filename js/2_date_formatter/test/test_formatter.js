var assert = require('chai').assert;
var DateFormatter = require('../date_formatter');

describe('DateFormatter', () => {
    describe('#parse()', () => {
        it('should parse date string without format', () => {
            assert.equal();
        });
    });
});